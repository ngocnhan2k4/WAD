<div class="grid md:grid-cols-3 grid-cols-2 gap-6">
    {{#each products}}
    <div class="bg-white shadow rounded overflow-hidden group">
        <div class="relative">
            <a href="#"><img src={{image}} alt="product 1" class="w-full"></a>
        </div>
        <div class="pt-4 pb-3 px-4">
            <a href="#">
                <h4 class="uppercase font-medium text-xl mb-2 text-gray-800 hover:text-primary transition">
                    {{name}}</h4>
            </a>
            <div class="flex items-baseline mb-1 space-x-2">
                <p class="text-xl text-primary font-semibold">{{discount_price}}</p>
                <p class="text-sm text-gray-400 line-through">{{original_price}}</p>
            </div>
        </div>
        <a href="#"
            class="block w-full py-1 text-center text-white bg-primary border border-primary rounded-b hover:bg-transparent hover:text-primary transition">Add
            to cart</a>
    </div>
    {{/each}}
</div>
<!-- Pagination controls -->
<div class="flex justify-center mt-10">
    <nav>
        <ul class="inline-flex items-center -space-x-px">
            {{#if hasPreviousPage}}
            <li>
                <a href="?page={{previousPage}}&sort={{sort}}&category={{category}}&brand={{brand}}&minPrice={{minPrice}}&maxPrice={{maxPrice}}&search={{search}}"
                    class="pagination-link py-2 px-3 ml-0 leading-tight text-white bg-primary border border-gray-300 rounded-l-lg hover:bg-primary hover:text-gray-700">Previous</a>
            </li>
            {{/if}}
            {{#each pages}}
                {{#if (shouldShowPage @index ../currentPage ../totalPages)}}
                    <li>
                        <a href="?page={{this}}&sort={{../sort}}&category={{../category}}&brand={{../brand}}&minPrice={{../minPrice}}&maxPrice={{../maxPrice}}&search={{../search}}"
                            class="pagination-link py-2 px-3 leading-tight text-white bg-primary border border-gray-300 hover:bg-primary hover:text-gray-700">{{this}}</a>
                    </li>
                {{else if (shouldShowEllipsis @index ../currentPage ../totalPages)}}
                    <li>
                        <span class="pagination-link py-2 px-3 leading-tight text-white bg-primary border border-gray-300">...</span>
                    </li>
                {{/if}}
            {{/each}}
            {{#if hasNextPage}}
            <li>
                <a href="?page={{nextPage}}&sort={{sort}}&category={{category}}&brand={{brand}}&minPrice={{minPrice}}&maxPrice={{maxPrice}}&search={{search}}"
                    class="pagination-link py-2 px-3 leading-tight text-white bg-primary border border-gray-300 rounded-r-lg hover:bg-primary hover:text-gray-700">Next</a>
            </li>
            {{/if}}
        </ul>
    </nav>
</div>